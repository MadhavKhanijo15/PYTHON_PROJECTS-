Reimplemented vanity_plates program, restructuring code , wherein 'is_valid' still expects a str as input and returns True if that str meets all requirements and False if it does not.
This program is based on unit testing.
Then, in a file called test_code.py, implemented functions that collectively test implementation of 'is_valid' thoroughly.
As to run this code make sure to download pytest library by writing command - pip install pytest.
