Program for those who have habit of making list of items from grocery store.

Program that prompts the user for items, one per line, until the user inputs control-d (common way of ending one’s input to a program). Then outputs the user’s grocery list in all uppercase, sorted alphabetically by item,
prefixing each line with the number of times the user inputted that item. The user’s input is treated case-insensitively  .
